package org.h2.ri.external.disk;

/*
 * This is a reference implementation of external tables still using MVStore format.
 * (We may implement one on top of parquet or AVRO to underscore that format is irrelevant)
 */
